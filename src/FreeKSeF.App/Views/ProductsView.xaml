<UserControl x:Class="FreeKSeF.App.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FreeKSeF.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:ProductsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Produkty}" SelectedItem="{Binding Wybrany}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" Width="*" />
                <DataGridTextColumn Header="J.m." Binding="{Binding Jednostka}" Width="60" />
                <DataGridTextColumn Header="Cena netto" Binding="{Binding CenaNetto, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="VAT" Binding="{Binding Stawka, Converter={StaticResource EnumLabel}}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="2">
            <TextBlock Text="Produkt / usługa" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

            <Label Content="Nazwa" />
            <TextBox Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}" />

            <Label Content="Jednostka" />
            <TextBox Text="{Binding Jednostka, UpdateSourceTrigger=PropertyChanged}" />

            <Label Content="Cena netto" />
            <TextBox Text="{Binding CenaNetto, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" />

            <Label Content="Stawka VAT" />
            <ComboBox ItemsSource="{Binding Stawki}" SelectedItem="{Binding Stawka}">
                <ComboBox.ItemTemplate>
                    <DataTemplate><TextBlock Text="{Binding Converter={StaticResource EnumLabel}}" /></DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Label Content="PKWiU / CN (opcjonalnie)" />
            <TextBox Text="{Binding Pkwiu, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Nowy" Command="{Binding NowyCommand}" />
                <Button Content="Zapisz" Command="{Binding ZapiszCommand}" />
                <Button Content="Usuń" Command="{Binding UsunCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
