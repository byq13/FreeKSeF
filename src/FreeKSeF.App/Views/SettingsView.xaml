<UserControl x:Class="FreeKSeF.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FreeKSeF.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Margin="0,0,0,6">
                <Run Text="Aktywna firma: " FontWeight="SemiBold" />
                <Run Text="{Binding AktywnaFirmaNazwa, Mode=OneWay}" Foreground="#1F2937" />
            </TextBlock>
            <DataGrid ItemsSource="{Binding Firmy}" SelectedItem="{Binding Wybrana}" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Firma" Binding="{Binding Nazwa}" Width="*" />
                    <DataGridTextColumn Header="NIP" Binding="{Binding Nip}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="560" HorizontalAlignment="Left">
                <TextBlock Text="Dane firmy (sprzedawcy)" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

                <Label Content="NIP" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding Nip, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="1" Content="Pobierz z GUS" Command="{Binding PobierzZGusCommand}"
                            Margin="8,2,0,6" VerticalAlignment="Center" />
                </Grid>

                <Label Content="Nazwa firmy" />
                <TextBox Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}" />

                <Label Content="Adres — linia 1 (ulica i numer)" />
                <TextBox Text="{Binding AdresL1, UpdateSourceTrigger=PropertyChanged}" />

                <Label Content="Adres — linia 2 (kod i miejscowość)" />
                <TextBox Text="{Binding AdresL2, UpdateSourceTrigger=PropertyChanged}" />

                <Label Content="Numer rachunku bankowego (domyślny)" />
                <TextBox Text="{Binding NrRachunku, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Połączenie z KSeF" FontSize="16" FontWeight="Bold" Margin="0,16,0,8" />

                <Label Content="Środowisko" />
                <ComboBox ItemsSource="{Binding Srodowiska}" SelectedItem="{Binding Srodowisko}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate><TextBlock Text="{Binding Converter={StaticResource EnumLabel}}" /></DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label Content="Token KSeF (wpisz, aby ustawić/zmienić)" />
                <PasswordBox x:Name="TokenBox" Padding="4,3" Margin="0,2,0,4" />
                <TextBlock Text="{Binding StatusTokenu}" Foreground="#6B7280" FontSize="12" Margin="0,0,0,8" />

                <TextBlock Text="Numeracja faktur" FontSize="16" FontWeight="Bold" Margin="0,16,0,8" />
                <Label Content="Szablon numeru (tokeny: {NR} {MM} {RRRR} {RR})" />
                <TextBox Text="{Binding NumerSzablon, UpdateSourceTrigger=PropertyChanged}" />
                <CheckBox Content="Reset licznika co rok (domyślnie co miesiąc)"
                          IsChecked="{Binding NumerResetRoczny}" Margin="0,6,0,0" />

                <WrapPanel Margin="0,12,0,0">
                    <Button Content="Nowa" Command="{Binding NowyCommand}" />
                    <Button Content="Zapisz" Command="{Binding ZapiszCommand}" />
                    <Button Content="Usuń" Command="{Binding UsunCommand}" />
                    <Button Content="Ustaw jako aktywną" Command="{Binding UstawAktywnaCommand}" />
                    <Button Content="Testuj połączenie" Command="{Binding TestujPolaczenieCommand}" />
                    <Button Content="Kursy walut (NBP)…" Click="KursyWalut_Click" />
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
